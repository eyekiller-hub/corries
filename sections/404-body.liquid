{% schema %}
  {
    "name": "404 body",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Title"
      },
      {
        "type": "richtext",
        "id": "body",
        "label": "Body"
      }
    ]
  }
{% endschema %}

{% assign title = section.settings.title | default: false %}

{% assign body = section.settings.body | default: false %}

<div class="padding-top-bottom--up-2 large--padding-top-bottom--up-4">
  <div class="container-default">
    {% if title %}
      <h1 class="margin-bottom--0">
        {{ title }}
      </h1>
    {% endif %}

    {% if body %}
      <div class="rte">
        {{ body }}
      </div>
    {% endif %}
  </div>
</div>
